version: "3.8"

services:
    app:
        build: .
        command: sh -c "python main.py"
        # Replace "YOUR_USER" for your Docker Hub user
        image: stefen2020/cicd-example:latest
        ports:
            - "80:8000"
